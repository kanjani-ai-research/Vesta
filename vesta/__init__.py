"""What a change touches.

A domain model of a codebase, and bounded propagation over it: given a change,
which things are actually affected. The claim is a correctness claim — "what
breaks" — because that is the only version anyone can score.
"""

__version__ = "0.1.0"
