---
description: State a rule for this project that Vesta could not have found
argument-hint: <the rule, in your own words>
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/vesta-run:*)
---
!`${CLAUDE_PLUGIN_ROOT}/bin/vesta-run learn --declare "$ARGUMENTS"`

Vesta recovers rules from corrections in earlier sessions, so a constraint the
user has simply always observed — never argued about, never corrected — leaves
no trace to recover. This records one outright.

Confirm what was recorded. Do not reword their rule.
