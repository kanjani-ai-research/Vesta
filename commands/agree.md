---
description: Agree to what Vesta will build, fixing the behaviour
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/vesta-run:*)
---
!`${CLAUDE_PLUGIN_ROOT}/bin/vesta-run contract --sign`

Show this verbatim. From here the agreed behaviour does not change: a request
that would alter it is a different project, not a revision.
