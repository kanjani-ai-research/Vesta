---
description: What a definition is for, and what else does the same kind of work
argument-hint: <name>
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/vesta-run:*)
disable-model-invocation: true
---
!`${CLAUDE_PLUGIN_ROOT}/bin/vesta-run means "$ARGUMENTS"`

Show this verbatim.
