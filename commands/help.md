---
description: What Vesta is and what you can do with it
argument-hint: [topic]
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/vesta-run:*)
disable-model-invocation: true
---
!`${CLAUDE_PLUGIN_ROOT}/bin/vesta-run guide "$ARGUMENTS"`

Show the guide above to the user verbatim. Do not summarise it or add to it.
