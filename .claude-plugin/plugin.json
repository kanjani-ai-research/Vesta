{
  "$schema": "https://anthropic.com/claude-code/plugin.schema.json",
  "name": "vesta",
  "description": "An adaptive ontology over a resolved code graph: the vocabulary is learned from this repository, the rules from its own users' corrections. Ask what a change touches, where a kind of work is done, what has already been decided, and what is worth fixing.",
  "version": "0.1.0",
  "author": {
    "name": "Causum"
  },
  "mcpServers": {
    "sidecar": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/vesta-sidecar"
    }
  }
}
